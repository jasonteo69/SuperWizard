# Copy to application.properties and edit for your environment
# Example for H2 (local dev)
db.url=jdbc:h2:./data/superwizard;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
db.user=
db.password=

# Example for Postgres (production)
# db.url=jdbc:postgresql://localhost:5432/superwizard
# db.user=superwizard
# db.password=secret
